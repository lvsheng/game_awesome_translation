/**
 * @author lvsheng
 * @date 2015/1/7
 */
define([
    '../../../util/resourceFileMap'
], function (resourceFileMap) {
    return cc.Layer.extend({
        ctor: function () {
            this._super(); this.init();

        },
        /**
         * @param scale 所生成方阵为几乘几的
         */
        generate: function (scale) {

        },
        whetherFind: function (position) {

        }
    });
});
